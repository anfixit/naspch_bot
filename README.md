# 🤖 naspch_bot

Telegram-бот для автоматической проверки текстов в чатах.
Проверяет орфографию, кастомные правила написания и пробелы.

## 📋 Возможности

### ✅ Что проверяет:
- **Орфография** — через Яндекс.Спеллер API
- **Кастомные правила** — "Гига чат" → "Гигачат"
- **Лишние пробелы** — множественные пробелы, пробелы вокруг пунктуации

### ⚙️ Особенности:
- Работает только с сообщениями райтеров (первая строка содержит `t.me/...`)
- Автоматическая перезагрузка конфигурации
- Игнорирование специфичных слов (эксплойт, хайп, инста...)
- Настраиваемый минимальный размер текста
- Гибкая настройка через JSON конфигурацию

## 🏗️ Архитектура
```
naspch_bot/
├── config/
│   └── bot_config.json      # Настройки проверок
├── src/
│   ├── bot.py               # Главный модуль бота
│   ├── text_checker.py      # Менеджер проверок
│   ├── checkers/            # Модули проверок
│   │   ├── base_checker.py
│   │   ├── spelling_checker.py
│   │   ├── custom_rules_checker.py
│   │   └── space_checker.py
│   └── utils/               # Утилиты
│       ├── config_loader.py
│       ├── formatter.py
│       └── message_validator.py
├── logs/                    # Логи бота
├── tests/                   # Тесты
├── .env                     # Переменные окружения
├── main.py                  # Точка входа
└── requirements.txt         # Зависимости
```

## 🚀 Установка

### 1. Клонируйте репозиторий
```bash
git clone <repository-url>
cd naspch_bot
```

### 2. Создайте виртуальное окружение (рекомендуется)
```bash
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env`:
```env
BOT_TOKEN=ваш_токен_бота
CONFIG_FILE=config/bot_config.json
LOG_LEVEL=INFO
```

### 5. Настройте бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. `Bot Settings` → `Group Privacy` → **Turn Off** (чтобы бот видел все сообщения в группе)

## ⚙️ Конфигурация

Файл `config/bot_config.json`:
```json
{
  "checks": {
    "spelling": true,
    "custom_rules": true,
    "spaces": true
  },
  
  "ignore_words": [
    "эксплойт",
    "хайп",
    "инста"
  ],
  
  "custom_rules": [
    {
      "wrong": "Гига чат",
      "correct": "Гигачат",
      "case_sensitive": false
    }
  ],
  
  "space_checks": {
    "multiple_spaces": true,
    "space_before_punctuation": true,
    "no_space_after_punctuation": true
  },
  
  "response": {
    "show_suggestions_count": 3,
    "show_emoji": true
  },
  
  "settings": {
    "min_text_length": 50
  }
}
```

### Добавление игнорируемых слов
```json
"ignore_words": [
  "эксплойт",
  "новое_слово"
]
```

### Добавление кастомных правил
```json
"custom_rules": [
  {
    "wrong": "Chat GPT",
    "correct": "ChatGPT",
    "case_sensitive": false
  }
]
```

## 🏃 Запуск

### Обычный запуск
```bash
python3 main.py
```

### Запуск в фоне (Linux/macOS)
```bash
nohup python3 main.py > logs/bot.log 2>&1 &
```

### Остановка
```bash
# Если запущен в терминале
Ctrl + C

# Если в фоне
ps aux | grep main.py
kill -9 [PID]
```

## 🧪 Тестирование

Отправьте в чат с ботом:
```
ТГ-канал (ауд 100К): t.me/testchannel

Новый продукт  появился в магазинах.Он стоит 
1000 рублей ,а работает через Гига чат.
Есть ошыбка в слове.
```

Ожидаемый ответ:
```
❌ Найдено ошибок: 4

📌 Неправильное написание:
1. «Гига чат» → «Гигачат»

📝 Орфография:
1. «ошыбка» → «ошибка»

⎵ Пробелы:
1. Лишние пробелы (2 подряд): «продукт  появился»
2. Нет пробела после ".": «магазинах.Он»
3. Пробел перед ",": «рублей ,»
```

## 🔧 Разработка

### Структура модулей

**Checkers** (проверки):
- `BaseChecker` — базовый класс для всех проверок
- `SpellingChecker` — орфография через Яндекс.Спеллер
- `CustomRulesChecker` — кастомные правила
- `SpaceChecker` — проверка пробелов

**Utils** (утилиты):
- `ConfigLoader` — загрузка и автообновление конфигурации
- `ErrorFormatter` — форматирование ответов
- `MessageValidator` — валидация сообщений

### Добавление нового чекера

1. Создайте класс, наследующий `BaseChecker`
2. Реализуйте метод `check(text: str)`
3. Добавьте чекер в `TextChecker._perform_checks()`

Пример:
```python
from .base_checker import BaseChecker

class MyChecker(BaseChecker):
    def check(self, text: str):
        errors = []
        # ваша логика
        return errors
```

## 📊 Логирование

Логи выводятся в консоль и могут быть перенаправлены:
```bash
python3 main.py >> logs/bot.log 2>&1
```

Формат логов:
```
[13:45:52] ✅ Конфигурация загружена
==================================================
[13:45:55] Проверяю сообщение от username
  → Проверка кастомных правил...
  → Проверка орфографии...
  → Проверка пробелов...
[13:45:56] Отправлен ответ
==================================================
```

## 🐛 Решение проблем

### Бот не отвечает в некоторых чатах

**Причина:** Privacy Mode включен

**Решение:**
1. @BotFather → /mybots → ваш бот
2. Bot Settings → Group Privacy → **Turn Off**

### Бот игнорирует сообщения

**Причина:** Сообщение не содержит `t.me/` в первой строке

**Решение:** Это нормальное поведение. Бот проверяет только сообщения райтеров.

### Изменения в конфиге не применяются

**Причина:** JSON невалидный

**Решение:** Проверьте JSON на [jsonlint.com](https://jsonlint.com/)

## 🔐 Безопасность

- ✅ Токен хранится в `.env` (не в коде)
- ✅ `.env` добавлен в `.gitignore`
- ✅ Используется `python-dotenv` для загрузки переменных

**Важно:** Никогда не коммитьте `.env` в git!

## 📦 Зависимости

- `python-telegram-bot==20.7` — Telegram Bot API
- `requests==2.31.0` — HTTP запросы к Яндекс.Спеллер
- `python-dotenv==1.0.0` — Загрузка переменных окружения

## 📝 TODO

- [ ] Unit-тесты для чекеров
- [ ] CI/CD pipeline
- [ ] Docker образ
- [ ] Поддержка нескольких языков
- [ ] Веб-интерфейс для настройки
- [ ] Статистика проверок

## 📄 Лицензия

MIT License

## 👤 Автор

Разработано для проверки текстов в Telegram-чатах Артёмом Платовым
и Анфисой Ковганюк

## 🤝 Контрибьюция

Pull requests приветствуются!

1. Fork проект
2. Создайте feature branch (`git checkout -b feature/amazing`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing`)
5. Откройте Pull Request
